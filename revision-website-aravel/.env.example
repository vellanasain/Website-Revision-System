# Frontend React (Vite)
VITE_API_BASE_URL=http://localhost:8080

# Backend Go API
PORT=8080
REVISION_DATA_PATH=backend-go/data/revisions.json

# Optional dev tunnel target.
# FRONTEND_TUNNEL_URL should point to Vite app (localhost:5173)
# BACKEND_TUNNEL_URL should point to Go API (localhost:8080)
FRONTEND_TUNNEL_URL=
BACKEND_TUNNEL_URL=
